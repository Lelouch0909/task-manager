# URL publique utilisée par le navigateur. Aucune donnée secrète dans VITE_*.
VITE_API_BASE_URL=/api
# Origine du backend, sans /api. Utilisée par Vite et par Vercel.
# Sur Vercel, définir cette variable dans Settings > Environment Variables.
API_PROXY_TARGET=http://localhost:8080
