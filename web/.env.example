# URL publique utilisée par le navigateur. Aucune donnée secrète dans VITE_*.
VITE_API_BASE_URL=/api
# Cible du proxy de développement Vite (sans le suffixe /api).
API_PROXY_TARGET=http://localhost:8080
